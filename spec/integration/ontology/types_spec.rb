require 'spec_helper'

describe "types" do
	
	# before :each do
		# @type = Factory(:type)
	# end
# 
	# it "listing all" do
		# visit types_path
		# page.should have_content(@type.name)
	# end
# 
	# it "visiting one" do
		# visit type_path(@type.id)
		# within(".wrapper h1") do
			# page.should have_content("Editing type")
		# end
	# end
end